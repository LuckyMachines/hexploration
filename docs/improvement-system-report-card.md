# Improvement System Report Card

## v0.1.0 - Baseline audit

The repository already had unusually capable specialist loops, but they behaved like excellent instruments without a shared dashboard. Evidence was abundant; portfolio-level prioritization and real-player closure were not.

| Dimension | Baseline | Evidence | Gap to a stricter A |
| --- | --- | --- | --- |
| Evidence capture | A- | Simulator, Oracle, memory, verification, marketing, UI, and art reports | No common freshness or confidence contract |
| Reproducibility | A- | Deterministic scripts and local checks | No single changed-surface entry point |
| Automated verification | B+ | Broad tests and hard verification tiers | Promotion was not coupled to evidence state |
| Prioritization | B | Many useful next actions | No ranked portfolio action across disciplines |
| Cross-system coherence | C+ | Specialist tools reference some shared artifacts | Grades, owners, and readiness terms were inconsistent |
| Real-player validation | C | Strong automated proxies | No structured recent-session threshold or consent-safe ledger |
| Promotion discipline | B | Art review and release checks contain gates | No shared legal state machine or decision ledger |

Overall baseline: **B**. Confidence: **high** in the diagnosis, **medium** in product quality because current human observation evidence is absent.

## Stricter A bar

Every meaningful change begins with a falsifiable hypothesis and ends with automatically captured before/after evidence, an explicit decision, a legal promotion state, and one ranked next action. Required evidence must be current, verification must be proportional to the changed surface, and at least five recent representative human sessions must cover first and return play.

## v1.0.0 - Control-plane implementation

| Dimension | Regrade | What changed |
| --- | --- | --- |
| Evidence capture | A | Shared evidence requirements, freshness, record thresholds, and compact/internal outputs |
| Reproducibility | A | One documented CLI, deterministic classification, scoped quick/full commands, tests |
| Automated verification | A- | Results are attached to affected surfaces and failures cap grades |
| Prioritization | A- | One ranked list across checks, missing evidence, human observation, staleness, and grade gaps |
| Cross-system coherence | A- | Eight surfaces share owners, objectives, grades, confidence, evidence, and stricter A bars |
| Real-player validation | C | Ledger and five-session gate now exist; genuine observations still must be conducted |
| Promotion discipline | A- | Sequential state machine requires evidence and an explicit decision |

Verified portfolio regrade: **B** overall with low confidence because representative human-play evidence is still empty. The improvement-system surface is **B+ with high confidence** after its unit suite and the cross-system smoke suite passed. The next regrade comes from generated portfolio evidence, not this document's assertion.

The first all-surface automated run recorded 100% scoped verification coverage, 0% regression rate, 86% required-evidence freshness, 88% implementation-checklist completion, one stale registered report, and zero human playtests. All selected automated checks passed. The low portfolio confidence is therefore localized to missing human observation rather than failing code.

## v1.1.0 - Bounded automatic repair

The default command now performs a diagnose-repair-verify-regrade loop instead of stopping at recommendations. Repair recipes are versioned, command-allowlisted, risk-bounded, write-path-declared, iteration-limited, non-repeating, and protected by a concurrent-run lock. Generated simulation, UI-density, marketing, SEO, and verification evidence can self-heal. Unsupported code changes, creative choices, and player observations are packaged for human or assisted work.

| Dimension | Regrade | Evidence |
| --- | --- | --- |
| Automatic remediation | A- | Bounded recipe selection, dry run, verification, and ineffectiveness detection |
| Change safety | A- | Declared write paths, dirty-file fingerprints, policy-violation stop, no automatic commit |
| Auditability | A | JSON and Markdown apply reports retain commands, outcomes, touched paths, and blockers |
| Human-truth protection | A | Human playtest deficits stop safely and cannot be filled by automation |

Improvement-system regrade: **A-**, pending repeated production use of successful repairs. Overall product portfolio remains **B** until observed-player evidence and the stale UX audit are resolved.

## Remaining blockers

1. Conduct and record five consent-safe representative sessions, including first play and return play.
2. Run the full control-plane gate before the next release candidate and capture the resulting portfolio.
3. Keep the hard gate in the explicit local/release workflow. GitHub Actions are intentionally not part of this repository's deployment policy.

## v1.2.0 - Current A- portfolio and hermetic hard gate

The current portfolio is **A-** with low confidence, up from the B baseline. Evidence freshness is 96%, verification and change-to-evidence coverage are both 100%, recommendation completion is 90%, and no registered reports are stale. Low confidence is now localized to missing observed-player sessions rather than failing automation.

| Dimension | Current result | Evidence |
| --- | --- | --- |
| Product and gameplay | A | Six exact checkpoints are current; the gameplay doctor and all truth gates pass |
| Game board | A | The sculpted six-family tile kit, interaction states, runtime budgets, and browser evidence pass |
| UX and accessibility | A- | Eleven scenes and five browser/device profiles pass with no serious accessibility findings |
| Marketing and discovery | A | Current marketing and SEO reports pass every automated gate |
| Technical reliability | A | The hermetic hard tier passed 19 of 19 commands with no failures, timeouts, or skips |
| Visual art | A- | Tile and material systems pass; a deterministic three-LOD gate promoted the Landing Beacon and Route Fork into the live board, while three relics and one shelter remain held for reconstruction |
| Operations and release | A- | Hard verification, exact-SHA live checks, rollback evidence, and dependency readiness are proven; this checkout is ahead of the deployed player SHA with uncommitted changes and is not yet a release candidate |
| Observed player validation | C | The consent-safe ledger exists, but genuine first-play and return-play sessions remain unrecorded |
| Improvement system | A | One command routes changed surfaces through proportionate checks, preserves verified evidence during previews, records decisions and legal promotions, and emits a ranked portfolio |

The verification runner now uses checked-in production configuration for production builds, starts its own pruned local chain before chain-dependent checks, allows the deterministic golden simulation enough time to finish, and writes smoke, focused, hard, exact, and release results to independent artifacts. A later smoke run can no longer overwrite the stronger hard-tier report.

### Remaining evidence gates

1. Observe and record five representative player sessions.
2. Reconstruct the Sunstone Lens, Atlas Spindle, Tideglass Heart, and Campsite Shelter; the review gate correctly prevents their current geometry from shipping.
3. Commit the reviewed work on top of deployed player SHA `eeee333cac319d9d2642ac9e00ffed516f12550f`, run the release tier from the clean commit, deploy through Coolify, and rerun the live gate with the exact expected SHA.

## v1.3.0 - Complete runtime art promotion

The visual-art surface now meets its stricter A bar. The governed runtime registry contains six approved assets and no retained candidates: Landing Beacon, Route Fork Marker, Campsite Shelter, Atlas Spindle, Tideglass Heart, and Sunstone Lens. Every asset has three fingerprinted delivery LODs, runtime budgets, five-angle contact-sheet evidence, and an explicit approval decision. The live board integrates the three relic identities deterministically with matching cutout fallbacks.

| Dimension | Current result | Evidence |
| --- | --- | --- |
| Visual and art direction | A | Art and material doctors pass; 3D tests pass 12/12; runtime doctor reports 6 approved and 0 retained; board report is Grade A |
| Game board | A | Chromium passes 28/28 and Firefox/WebKit pass 12/12 with stable transforms, bounded camera controls, context recovery, and forced-colors support |
| Runtime performance | A | The 100-tile scene remains at 86 draw calls, 10,377 triangles, 64 textures, 2.4 ms render p95, and zero asset failures |
| Automated UX and UI | A | Eleven approved responsive scenes and all first-action, keyboard, reflow, forced-colors, reduced-motion, assistive, and touch contracts pass |

The remaining portfolio confidence gap is intentionally not hidden: representative human observation and production telemetry are still absent, and release promotion still requires a reviewed commit, Coolify deployment, and exact-SHA post-deploy verification.

## v1.4.0 - Lossless runtime image delivery

The approved PNG masters remain the provenance source, while 23 board-facing character, prop, effect, encounter, and relic images now ship as fingerprinted lossless WebP derivatives. The primary art manifest explicitly delegates those outputs to the runtime-delivery manifest, so strict orphan detection remains active across the entire art tree.

| Dimension | Current result | Evidence |
| --- | --- | --- |
| Package efficiency | A | 9.52 MiB of canonical PNG masters deliver as 5.68 MiB of lossless WebP, a 40.3% reduction |
| Production route transfer | A | The local production build reduced projected image transfer by 25.3% on `/` and 24.0% on `/guest` without server compression |
| Visual fidelity | A | The complete UI screenshot suite remains pixel-identical after the runtime source switch |
| Pipeline governance | A | 17/17 art contract tests pass; hashes, dimensions, byte ratios, and derivative ownership are checked |
| Board performance | A | The 100-tile scene remains within its Grade A render budget and reports zero asset failures |

The product portfolio remains **A- with low confidence**. The remaining gap is evidence outside this implementation pass: real observed-player sessions, production telemetry, and a reviewed release commit followed by Coolify and exact-SHA verification.

The post-change hard gate passed all 19 stages with zero failures, timeouts, or skips, including the production build, full application suite, Forge contracts, exact local stack, and deterministic golden simulation. The live production audit then passed all 12 applicable checks against deployed SHA `eeee333cac319d9d2642ac9e00ffed516f12550f`; sponsor-relay scope remains inapplicable to that older deployment because it predates capability metadata.

## v1.5.0 - Domain-role and release-candidate truth gates

Marketing and player entry now have an explicit anti-duplication contract. `xenovoya.com` owns explanation and proof; `play.xenovoya.com` identifies itself as the playable client and immediately offers solo, observe, and crew entry. The refreshed main marketing doctor passes 10/10, the SEO doctor passes, and the separate marketing-site quality doctor passes 12/12 at A with high confidence.

Release readiness now has two distinct gates. The new local candidate audit requires a clean `main` checkout, a full expected SHA matching `HEAD`, fresh zero-skip hard-suite evidence from that same clean commit, matching production build metadata, and a valid Coolify rollback contract. The existing live audit remains the post-deploy exact-SHA and service-health gate.

The current candidate audit intentionally returns **NOT READY / B**: branch, expected SHA, hard-suite result, build identity, and rollback contract pass, while checkout cleanliness and clean hard-evidence provenance fail. This is correct evidence for the current uncommitted workspace and prevents an A release claim before review and commit.

The strict live gate now also requires exact expected SHAs for both independently deployed repositories, validates the marketing release manifest, and applies the full security-header contract to both domains. A live read-only audit scored **B (12/16 passed)** because the current marketing deployment lacks six required headers and three promotion-only checks were intentionally skipped. The checked-in Coolify nginx configuration now supplies CSP, HSTS, permissions, referrer, MIME-sniffing, framing, opener, and resource policies while retaining immutable caching for fingerprinted assets. The exact container passed 8/8 header assertions plus cache and release-identity checks. This gap remains a production failure until the reviewed marketing commit is deployed.
